<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.faudroids.werewolf"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.WerewolfApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.ShowRolesActivity" />
        <activity android:name=".ui.GameSetupActivity" />
        <activity android:name=".ui.PlayersOverviewActivity" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="d24b6175dc50a0705e30d49f9f580ca61cbddbed" />
    </application>

</manifest>
